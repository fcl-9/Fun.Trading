name: .NET CD

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      CONTAINER_IMAGE_NAME: "fun-trading-api"
      CONTAINER_IMAGE_TAG: latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
        
      - name: Setup .NET Core SDK
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: '8.0.x'
        
      - name: Restore dependencies
        run: dotnet restore
        
      - name: Build
        run: dotnet build --configuration Release
        
      - name: Run tests
        run: dotnet test --configuration Release --no-build
        continue-on-error: true
        
      - name: Upload test results
        if: ${{ always() }}
        uses: actions/upload-artifact@v2
        with:
          name: test-results
          path: '**/TestResults/*.trx'

      - name: Build Container Image
        run: docker build -t "${CONTAINER_IMAGE_NAME}":"${CONTAINER_IMAGE_TAG}" . -f "./docker/Dockerfile"

      - name: Save Container Image
        run: docker save --output image.tar "${CONTAINER_IMAGE_NAME}":"${CONTAINER_IMAGE_TAG}"
      
      - name: Upload Container Image
        uses: actions/upload-artifact@v4
        with:
          name: container-image
          path: image.tar
          retention-days: 1


      # Example: Push Docker image to registry (replace with your deployment steps)
      # - name: Push Docker image
      #   run: |
      #     echo ${{ secrets.DOCKER_PASSWORD }} | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin
      #     docker push your-image-name

      # # Add steps for deploying your application (replace with your deployment steps)
      # - name: Deploy to production
      #   run: |
      #     # Example: Use kubectl to apply Kubernetes manifests
      #     kubectl apply -f deployment.yaml
